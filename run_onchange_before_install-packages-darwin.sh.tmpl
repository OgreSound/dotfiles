{{- if eq .chezmoi.os "darwin" -}}
#!/bin/zsh
# .Brewfile hash: {{ include ".Brewfile" | sha256sum }}
echo "Installing Homebrew packages from .Brewfile..."
brew bundle install -v --file="{{ .chezmoi.sourceDir }}/.Brewfile"
{{ end -}}